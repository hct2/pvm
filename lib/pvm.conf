#!/usr/bin/perl

use warnings;
use strict;

our ($VERSION) = (q$Revision: 67 $ =~ /(\d+)/msx);

our $pvm_conf = {
  hmc_names => [qw/hmc1/],	# for example: [qw/hmc1 hmc2 hmc3/],
  hmc_user => 'hscroot',
};

1;
